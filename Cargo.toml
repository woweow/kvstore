[package]
name = "kvstore"
version = "0.1.0"
edition = "2021"
description = "A simple key-value store with TTL support"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0" 